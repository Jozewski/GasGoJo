// Web implementation — the Maps API key is injected via index.html <script> tag.
// No additional initialization needed here.
Future<void> initializeMapsApiKey(String apiKey) async {
  // Key is already loaded via the Google Maps JS script in web/index.html
}
